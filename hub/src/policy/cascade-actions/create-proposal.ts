/**
 * Retired Cascade ActionSpec: create_proposal.
 *
 * proptool0 removes Proposal creation from thread convergence. This tombstone
 * keeps the historical file path discoverable while intentionally registering
 * no ActionSpec; importing cascade-actions/index.ts no longer imports this file.
 */
export {};
