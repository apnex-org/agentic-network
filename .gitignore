# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
*.tgz

# Logs
*.log
.ois/

# Gitignored local state directory — local-fs storage-mode artifacts
# (a dev/test affordance per CLAUDE.md) + adapter-side local state. The
# production Hub + scripts/local/start-hub.sh run the postgres substrate;
# the GCS↔local-fs cutover + scripts/state-sync.sh were removed at the
# mission-83/84 substrate cutover.
local-state/

# Personal host configuration (not shipped)
.claude/
.opencode/
/.mcp.json

# Secrets
labops-389703.json
*.pem
*.key
.env
.env.*

# Backups
backups/

# Terraform
*.tfstate
*.tfstate.backup
.terraform/

# Personal scratch
notes.txt
raw-notes
raw-notes.txt

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Per-worktree .npmrc (env-var ref to NPM_TOKEN; never commit auth)
.npmrc
__pycache__/

# transient: staged by stage-skills.js during npm pack (postpack cleans)
/adapters/claude-plugin/skills/
