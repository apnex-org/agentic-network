# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
*.tgz

# Logs
*.log
.ois/

# local-fs Hub state directory — default OIS_LOCAL_FS_ROOT for
# `scripts/local/start-hub.sh` when STORAGE_BACKEND=local-fs. Grown
# organically by the Hub during local-fs operation. (local-fs is now a
# dev/test affordance only — production runs the postgres substrate;
# scripts/state-sync.sh + GCS were removed at the mission-83 cutover.)
local-state/

# Personal host configuration (not shipped)
.claude/
.opencode/
/.mcp.json

# Secrets
labops-389703.json
*.pem
*.key
.env
.env.*

# Backups
backups/

# Terraform
*.tfstate
*.tfstate.backup
.terraform/

# Personal scratch
notes.txt
raw-notes
raw-notes.txt

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Per-worktree .npmrc (env-var ref to NPM_TOKEN; never commit auth)
.npmrc
