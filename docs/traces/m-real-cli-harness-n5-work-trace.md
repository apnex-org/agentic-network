# Work-trace — M-Real-CLI-Harness n5 (full container integration + 3-level sentinel e2e)

**Node:** `work-bp-real_cli_harness_20260630-n5_container_sentinel_e2e` (engineer; arc idea-405). **Evidence:** `ev_container_e2e` (test-run) + the **relocated `ev_engineer_ready`-BEHAVIORAL** (architect carry-forward from n4). **Branch:** `agent-greg/real-cli-n5-container` (stacked on `agent-greg/real-cli-n4-ready` = PR #444; rebase onto main when #444 merges).
**Lease token (full):** `dc88a6d7-57ab-43c7-b2cb-507a031670c3`.

## Deliverables (runbook + lily carry-forward, msgs 01KWBK63CP / 01KWBK7MT5)
1. **Image on the PROVEN plugin path** — the EMBEDDED container: `supervisor.mjs` → real `claude` CLI (under tmux/PTY, n1 mode A) → `@apnex/claude-plugin` shim **GRANDCHILD**. Flags: `--dangerously-skip-permissions --dangerously-load-development-channels plugin:agent-adapter@agentic-network` (TWO sandbox-bounded dangerous flags; n6 certs both). Bounded sandbox (n2) + dialog-free boot (n3) + L1.5 watchdog ON + `/run` tmpfs mode=1777.
2. **`ev_engineer_ready`-BEHAVIORAL acting-confirm** (relocated from n4, substrate-bound, HARD): an injected `work_claimable_digest` drives the REAL CLI through `claim→start→complete + renew_lease` on a SEEDED item, **ZERO-human**, in-container, liveness ON. **BOOTSTRAP finding (the headline lily brings the Director): unprimed-first** — does the raw CLI self-drive off the digest, or need minimal engineer priming? Report which.
3. **Positive `<channel>` render capture** (the n4 capstone, folded here — the render the local receipt couldn't positively capture).
4. **`ev_container_e2e`: the 3-level sentinel chaos** — re-run the P1e-2 silent-wedge with the REAL CLI in the middle: L1.5 probe-fail → shim writes `/run/adapter-wedged` + self-exit → supervisor `exit(75)` → docker-L2 restart → real CLI re-boots headless → re-handshake → re-claim, ZERO-manual. + SIGTERM/SIGKILL cleanly tears down the CLI + grandchild.

**SSE = WATCH-not-build** (n4 finding): the no-quota SDK probe proved the test-Hub SSE delivers fine (45s alive + 30s keepalive); the shim's 17s drop was a non-faithful `OIS_LIVENESS_WATCHDOG_ENABLED=0` (root cause UNCONFIRMED). Run the agent prod-faithful (liveness ON). A recurrence here is an n5 FINDING; `maxRetries:0` ⇒ a real drop is the KERNEL reconnect path.

## Plugin-bake — RESOLVED (in-repo marketplace; no external hand-staged dir)
`adapters/claude-plugin/.claude-plugin/` already carries **`marketplace.json`** (name `agentic-network`, plugin `agent-adapter`, source `./`) + `plugin.json`. The pilot Dockerfile already `COPY`s `adapters` → `/app/adapters`. So the image registers `/app/adapters/claude-plugin` as a **directory-source marketplace** in the baked preseed (known_marketplaces.json) + enables `agent-adapter` — NOT the external `/home/apnex/apnex-claude-plugin/package`. (Greg's host uses the external dir; the image uses the in-repo equivalent.)

## Image deltas (runtime stage, over the pilot Dockerfile)
- `apt-get install tmux` (PTY host for interactive claude — n1 mode A).
- `npm i -g @anthropic-ai/claude-code@2.1.196` (PINNED — the real CLI).
- Bake the n3 preseed (`claude-cli-preseed.claude.json`) into appuser's `CLAUDE_CONFIG_DIR` + register the `agentic-network` directory-marketplace (→ `/app/adapters/claude-plugin`) + enable `agent-adapter` + project-trust + MCP-approval (the n4-receipt config-patch pattern).
- **CMD flip:** supervisor child = the real-CLI launcher (tmux + `claude` with the two flags) — must keep a FOREGROUND child so the supervisor watches its exit (tmux-detached returns immediately → needs a keep-alive wrap, OR run claude in the container PTY). DETAIL to settle in the build.
- Build via the pilot's **Cloud Build** path (local docker-run core-dumps node images).

## Run-shape (proposed; awaiting lily steer)
ONE container on the VM, sequenced: boot (dialog-free) → handshake (register engineer) → **behavioral acting-confirm + `<channel>` render capture** (the seeded digest drives claim→complete, unprimed-first) → **then the wedge-chaos** (3-level recovery e2e). Shares the n4 test-Hub-with-work bundle as the network-scoped Hub. VM-timing: build the image (Cloud Build) FIRST, then ping lily for the parked-VM restart (n4 step-4 pattern).

## Log
- **2026-06-30 ~09:10Z — ✅ n5 DONE (complete_work ev_container_e2e; PR #446 stacked on #444).** The FULL 3-level sentinel chaos PROVEN on the VM, ZERO-human (image `claude-adapter-realcli:n5-ac9af35`): the **(b) faithful reject-handshake wedge** (architect steer — model the L1-UNRECOVERABLE failure L2 EXISTS FOR, not eviction-density gaming the 2s-probe; the 50ms-evict L1-recovery was the DESIGN WORKING) → L1.5 watchdog 2-consecutive-fail → `[supervisor] wedged-restart sentinel appeared -> terminating child + exit(75)` → docker-L2 restart (RestartCount 0→1) → re-boot → **launcher auto-accepted the dev-channels dialog (gate-3.5, zero-human)** → shim re-spawn → re-handshake → re-register (epoch=2) → the recovered CLI claimed+completed the FRESH `work-2`. **CLEAN SIGTERM teardown** = `received SIGTERM -> terminating child + exit(0)` (Running=false Exit=0, no loop). Evidence persisted to host /work. **Three shim-spawn fixes** found live: settings.json{skipDangerousModePermissionPrompt} (bypass dialog) + the launcher dev-channels auto-accept (gate-3.5) + the shim needs deps (dir-source `/app/adapters/claude-plugin` resolves `/app/node_modules` natively → self-contained image, no symlink) **+ a non-empty OIS_HUB_TOKEN** (the shim's cred-check; dummy added to the compose). Cred trap-removed from the VM post-run (exposure window closed). **n1-n5 ALL DONE; n6 (steve accept-gate) is the terminal gate.**
- **2026-06-30 ~08:35Z — 🎯 MARQUEE PROVEN: ev_engineer_ready-BEHAVIORAL CLEARED (the headline).** With the Director-staged OAuth cred, the UNPRIMED real CLI self-drove the ENTIRE flow off the self-wake digest, ZERO-human, ZERO-priming, in-container, liveness ON. Pane transcript: the digest RENDERED via `<channel>` ("← proxy: [Hub] 1 item now claimable for the engineer role") → "This is a connectivity self-test work item. Let me claim it" → claim_work → Write(/work/n5-proof.txt = N4-ENGINEER-PROOF-OK) → **REASONED OUT THE FSM** ("The item needs to move from claimed → in_progress first") → start_work → complete_work → "✅". Seeded item: status=DONE, holder=agent-29beb4c8, evidence:[{self-test, freeform}]. **BOOTSTRAP FINDING = UNPRIMED-SELF-DROVE** (no priming needed; the digest + the work-item runbook alone, with emergent FSM competence). Evidence PERSISTED to host /work (before the wedge, per the guardrail): `n5-proof.txt` + `n5-workitem-final.json` + `n5-acting-confirm-transcript.txt`. `<channel>` render = the n4 capstone, confirmed live e2e.
  - **THREE boot gates found (the real 'automate the startup' arc-concern, landed):** (1) bypass-permissions dialog → suppressed by `settings.json{skipDangerousModePermissionPrompt}` (bootstrap fix); (2) dev-channels confirmation dialog → POST-LOGIN, NON-persisting (gate-3.5) → **launcher auto-accept** (poll-pane + Enter, added to real-cli-launch.sh — THE self-RECOVERS-unattended property for n6); (3) shim-MCP-spawn → was `✘ failed` for TWO reasons: ERR_MODULE_NOT_FOUND (plugin cache copy lacks node_modules; repo plugin's deps hoisted to /app/node_modules — claude actually ran the dir-source shim at /app/adapters/claude-plugin/dist/shim.js where deps resolve NATIVELY, so the OPERATIVE fix was the next one + the image is self-contained, no symlink), and "Hub credentials not found" → the shim requires a NON-EMPTY OIS_HUB_TOKEN even for the no-auth test-Hub → dummy token added to the compose.
  - **CRED bind-mount UID fix:** the Director-staged .credentials.json was 0600/sa-owned → unreadable by the container appuser (uid 10001) → `chown 10001:10001` (the classic bind-mount UID-mismatch).
- **2026-06-30 ~08:44Z — WEDGE-CHAOS: L1 recovered, L2 did NOT fire (lily's seam; steer requested).** POST /wedge (evicted 2, sustaining 50ms/12s) → the kernel L1 wire-reconnect recovered the session (epoch=2, wasCreated=false) BEFORE the L1.5 watchdog hit its budget. Shim log: `[LivenessWatchdog] probe FAILED (1/2)` → `[L1 reconnect] reconnecting → synchronizing` → `session re-confirmed live; failure counter reset (was 1)` → never 2/2 → no sentinel → no exit-75 → RestartCount stayed 0. **Root cause:** the 50ms-eviction has gaps the 2s-probe slips through + L1 reconnect is fast. **FIX PREP:** made the wedge eviction cadence configurable (`WEDGE_INTERVAL_MS`, default 50) so a tighter cadence (~5ms) can DEFEAT L1 → drive L1.5→sentinel→L2. Awaiting lily's steer: (a) tighten eviction vs (b) reject-reconnect server-side. Then rebuild image (launcher auto-accept) + re-run the wedge-chaos for the L2 path + the zero-human re-handshake.
- **2026-06-30 ~07:19Z — VM no-OAuth PREP done + 2 bootstrap gaps fixed + a login-gating finding (reported lily 01KWBPBDYN…).** VM adapter-p1e2-e2e (australia-southeast1-a, sudo-docker 29.6.1, gsutil). Delivered the prep tarball via gsutil; pulled the n5 image (after `gcloud auth print-access-token | sudo docker login` — registry auth); ran the test-Hub-with-work + compose-up. **DE-RISKED (OAuth-independent):** image+boot **dialog-free** ✓ + **plugin-install** ✓ + supervisor→launcher→tmux→claude wired ✓. **TWO bootstrap GAPS found+fixed:** (1) `settings.json{skipDangerousModePermissionPrompt:true}` was MISSING → claude blocked on the `--dangerously-skip-permissions` Bypass-accept dialog (defaults to "No, exit") — added to `real-cli-bootstrap.cjs` (committed). (2) the plugin PATH needs the plugin **INSTALLED**, not just the marketplace registered (greg's `~/.claude` has `installed_plugins.json`); `claude plugin install agent-adapter@agentic-network` works **WITHOUT OAuth** (directory-marketplace copy) — added to the orchestrator PHASE-0 (idempotent install + restart). **LOGIN-GATING FINDING (refines lily's shim-OAuth-independence):** not-logged-in, the pane shows `--dangerously-load-development-channels ignored` + `Channels are not currently available`, and NO shim process spawns — so claude does NOT activate the plugin's MCP server (shim) or channels until OAuth. The shim's Hub-HANDSHAKE protocol is OAuth-independent (OIS_HUB_TOKEN), but claude SPAWNING the shim + channels is login-gated → register_role(engineer) is verifiable only WITH the cred (the full RUN). **CRED-gate cleared** (Director approved: host OAuth on the bounded VM, destroy-after); lily stages `.credentials.json` into the mount `/home/sa_112119950551775486548/n5/claude-cfg/.credentials.json` (→ container `/home/appuser/.claude`); plugin+settings+preseed+marketplace already staged there. **NEXT:** lily's cred → the full RUN (boot→login→shim-spawn→register→self-wake digest→unprimed-first claim/complete + `<channel>` render→persist /work→wedge-chaos).
- **2026-06-30 ~06:50Z — n5 IMAGE BUILT + VERIFIED (Cloud Build).** `claude-adapter-realcli:n5-bad98e5` (build `4129ed6c` SUCCESS). Feature-presence VERIFIED via a Cloud Build run-check (`cloudbuild.verify-realcli.yaml`, build-provenance lesson honored): **claude 2.1.196** + **tmux 3.3a** + node 22 + the launcher `/app/real-cli-launch.sh` + the shim `/app/adapters/claude-plugin/dist/shim.js` (grandchild) + the in-repo marketplace baked (`known_marketplaces.json: agentic-network DIRECTORY → /app/adapters/claude-plugin`) + dialog-free `.claude.json` (hasCompletedOnboarding + projects:['/work']). Image artifacts committed (c8cb72e + bad98e5 staging-fix). e2e prep committed (a7af5d1: `docker-compose.real-cli.yml` + the test-Hub `/wedge` endpoint + `evictAllTransports`). **Pinged lily for the parked-VM restart** (msg 01KWBMPB39…) + flagged two coordination items: (1) the §5 OAuth creds-staging ON the bounded VM (Director-auth gate if needed), (2) the IAP-SSH/gsutil VM-access pattern (same as p1e2-e2e.sh). **NEXT (gated on lily's VM):** the e2e orchestrator (`real-cli-e2e.sh`) — boot test-Hub-with-work + the n5 container → behavioral acting-confirm (UNPRIMED-FIRST) + `<channel>` render capture → PERSIST to /work (guardrail) → wedge-chaos 3-level recovery.
- **2026-06-30 ~06:31Z** — n4 DONE + PR #444 (test+docs+deploy-script, no hub/** runtime); n5 claimed + started; branch `agent-greg/real-cli-n5-container` (stacked on n4). Plugin-bake RESOLVED (in-repo marketplace). Image deltas + run-shape scoped; surfacing the plan + run-shape/VM-timing to lily, then building the Dockerfile/compose delta.
