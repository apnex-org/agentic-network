# mission-86 M-Hub-Storage-Cloud-Deploy — Architect Trace / Cold-Pickup Handover

**Role:** architect (lily). **Mission:** mission-86 M-Hub-Storage-Cloud-Deploy, Phase 8 (wave execution).
**Written:** 2026-05-20 — an architect-side context-handover. A fresh lily session: read this first, then the load-order below.

## Cold-pickup load order
1. **This doc** — the live operational state.
2. **`docs/designs/m-hub-storage-cloud-deploy-design.md`** — Design **v2.11 RATIFIED** (on the architect branch `agent-lily/m-hub-storage-cloud-deploy`). The authoritative spec — read §5 (waves W0–W5) + the "bug-103 slice" section. v2.11 = W5 soak descoped.
3. **thread-603** — the ACTIVE channel (W5-close — local-Hub decommission + mission close). thread-601 (W4-cutover) + thread-602 (W5 validation) are CONVERGED. NOTE: `get_thread` caps at 10 messages — open a fresh thread at ~10 (thread-600→601→602→603 pattern); `list_messages` with a `since` cursor DOES work on the v0.1.4 adapter for paging.
4. Entities: `get_mission mission-86`; bugs 101–107; idea-306.
5. CLAUDE.md + `docs/methodology/{mission-lifecycle,architect-runtime}.md`.

## Mission state — 2026-05-21

> **mission-86 CLOSED 2026-05-21 — status `completed`. This trace is terminal.** All waves W0–W5 landed; W5-close PR #231 merged → `main @ 579dace`; production Hub on GCP, local Hub decommissioned + archived, zero data lost. Phase 10 retrospective walked live (bilateral with the Director): **calibrations #79–82 filed** to `docs/calibrations.yaml` (assert-from-incomplete-evidence · dry-run-doesn't-exercise-the-remote-surface · state-cutover-misses-operator-credentials · constrained-platform-toolchain-discovered-reactively); the `/tmp`-install footgun folded into idea-307; **idea-306 + idea-307 held for the next Strategic Review** (Director has additional next-version requirements to fold there). thread-601/602/603 all converged. **Open close-time follow-ups (non-blocking; operator/Director-coordinated):** the TF token-output `terraform apply`; a GCS `archive/` no-expiry lifecycle rule.

- **W0 / W1 / W2 / W3 + the bug-103 slice — DONE + merged to `main`.** Merged PRs: #220 (W1 VM/COS), #221 (W2 state-migration + bug-101), #222 (W3 Sub-PR A — Secret Manager + Cloud NAT + bridge), #223 (W3 Sub-PR B — bearer-auth gate + bug-102), #224 (bug-103 slice — bug-104 + bug-103-(D) + bug-106), #225 (W4 cutover-to-cloud.sh + rollback runbook), #226 (W4 `--yes` flag).
- **W4 — production cutover — COMPLETE. Cloud Hub IS production. W4-closeout merged.**
  - Attempt #1: aborted safely at `confirm()` (interactive-prompt/stdin) → fixed by #226 (`--yes`).
  - Attempt #2: DRAIN→SNAPSHOT→UPLOAD clean, **failed at RESTORE** (`gsutil` not on COS); clean Scenario-A rollback, zero data loss.
  - **Attempt #3 — SUCCEEDED 2026-05-21.** `cutover-to-cloud.sh --yes` ran end-to-end clean; parity 18541==18541. **The cloud Hub at `https://hub-api-5muxctm3ta-ts.a.run.app` is now production.** Local Hub `ois-hub-local-prod` stopped (`Exited(0)`, graceful — NOT removed; W5 decommission). **All 7 AG-W4 gates verified ✅** (W4.1/4.2/4.5/4.7 at cutover; W4.3 `activeSessions:2`, W4.4 `list_missions` clean, W4.6 both configs on cloud `/mcp` URL post-restart).
  - **Step-3 bearer-auth blocker — RESOLVED.** greg root-caused: the adapter-config `hubToken` was the local Hub's `HUB_API_TOKEN` *grandfather env token*; the cloud Hub's `HUB_API_TOKEN` is a separate Secret-Manager `random_password` (env state, not DB — `pg_restore` could not carry it). Gap: step 3 = flip URL **+ token**, but `adapter_flip_notice()` only specified the URL. Fixed.
  - **W4-closeout — PR #228 MERGED → `main @ c346d5d`** (architect cross-approved; all 5 required CI gates green). `adapter_flip_notice()` rewritten to instruct both `hubUrl`+`hubToken` with read commands + 200-check; TF `hub_api_token`+`admin_token` sensitive outputs added.
  - **W4-closeout tail (open):** (1) non-destructive `terraform apply` to materialise the TF token outputs — operator/architect-coordinated; surfaced to Director 2026-05-21; not W5-blocking. (2) stale `start-{greg,lily}.sh` header comments — untracked operator-owned scripts, not a repo deliverable; surfaced to operator.
- **W5 — IN PROGRESS on thread-602.** greg's W5 plan architect-CONCURRED 2026-05-21. AG-W5.1 descoped → idea-306.
  - **SOAK DESCOPED — Director-direct 2026-05-21:** AG-W5.2 (~24h uptime smoke) removed — "not part of the mission." **Design amended → v2.11** (commit `8bda9c8`). W5 critical path collapses to [plugin-release → AG-W5.9].
  - **PRODUCTION ADAPTER UPDATE — Director GO 2026-05-21.** greg cut + published **release `v0.1.4`** (annotated tag on `main @ 359738f`; first real `m-github-releases-plugin-distribution` release; `release-plugin.yml` run success). Post-publish dogfood of the published `.tgz` PASSED — bug-103 adapter-half (`firstTimerEnabled`) + bug-106 fix (`isInternalCall`) confirmed present; the message-router build-cycle annotation triaged benign (dist complete). Architect-signed-off. **Architect ran the v0.1.4 install 2026-05-21** (Director-asked) — extracted to `/tmp/plugin-v014` + ran `install.sh` from there.
  - **INCIDENT (2026-05-21): post-restart both adapters FAIL TO LOAD.** Both sessions restarted correctly (`ps` confirms `claude … plugin:agent-adapter@agentic-network`), but the `agent-adapter` MCP server does not spawn — `mcp__plugin_agent-adapter_proxy__*` tools gone, no `node shim.js` process, shim logs frozen at the pre-restart shutdown, cloud Hub `activeSessions:0`. Both agents OFF the network.
  - **ROOT CAUSE (architect error):** `install.sh` was run from `/tmp/plugin-v014/package`; `claude plugin marketplace add` recorded the `agentic-network` marketplace `source` + `installLocation` as that temp path (confirmed in `~/.claude/plugins/known_marketplaces.json`). `/tmp/plugin-v014` was reaped → dead marketplace reference → Claude Code cannot resolve/load the plugin at session start. The v0.1.4 adapter CODE is GOOD — the cached shim (`~/.claude/plugins/cache/agentic-network/agent-adapter/1.0.0/dist/shim.js`) runs + connects to the cloud Hub cleanly when invoked directly.
  - **FIX APPLIED + VERIFIED 2026-05-21.** (1) `claude plugin uninstall` + `claude plugin marketplace remove agentic-network` — dead `/tmp` registration cleared; (2) re-extracted v0.1.4 to STABLE dir `/home/apnex/apnex-claude-plugin/package`; (3) `install.sh` re-run from there — clean. `known_marketplaces.json` source now the persistent stable path.
  - **RESOLVED 2026-05-21 — operator restarted both sessions; both adapters reconnected.** Cloud Hub `/health` → `activeSessions: 2`. The v0.1.4 adapter loaded cleanly (the `agent-adapter:survey` plugin-namespaced skill is registered). The production adapter update is COMPLETE. **Next: greg runs AG-W5.9 on thread-602 (turn is greg's).**
  - **NOTE — `claude plugin list` shows version `1.0.0`** (not `0.1.4`): the Claude-plugin manifest `.claude-plugin/plugin.json` has a static `version: 1.0.0`; the release/`.tgz` track `package.json` (`0.1.4`). Labelling drift only — installed code is `commitSha 359738f` (correct). Folded into idea-307 (native-versioning sub-item).
  - **idea-307 UPDATED 2026-05-21** via `update_idea` — version-skew sub-finding + the /tmp-install-footgun operational note folded into idea-307's text.
  - **AG-W5.6** (rollback-runbook v1.1) + **AG-W5.7** (bug-101 → resolved) DONE — **PR #229 merged → `main @ 359738f`**.
  - **AG-W5.3** (backup-runner verify) — satisfied-by-evidence: greg confirmed 22 hourly GCS snapshots, post-cutover ones full-size (~8.8 MB = migrated production state); the mechanism is proven (≥24 was a soak-coupled number).
  - **AG-W5.9 — PASS (bilateral, 2026-05-21).** Probe PR #230 (docs-only no-op) cross-approved + merged → `main @ bd95872`. **BOTH event-halves push-delivered to the lily inbox, NOT stranded:** `pr_opened` (`01KS4AYZHAFD06M7A82C4XCT51`) + `pr_merged` (`01KS4BDYCEKE0V2537TEYNENMC`); greg also got the live `pr_review_approved`. bug-103 end-to-end production proof COMPLETE. AG-W3.12 (harness) + AG-W5.9 (real-adapter) both closed. greg declared PASS, architect concurred.
  - **thread-602 CONVERGED 2026-05-21** (10-msg cap); W5-close tail moved to **thread-603**.
  - **AG-W5.4/W5.5 — decommission plan APPROVED 2026-05-21 (thread-603).** greg surveyed: `ois-hub-local-prod` `Exited` since W4.5; `hub-substrate-postgres` frozen-healthy (data on volume `hub-substrate-data`). Plan: W5.5 final-state archive FIRST (`hub-snapshot.sh` pg_dump → GCS `archive/mission-86-final-local-hub-state-<ts>.dump`, path-option (a) — dedicated `archive/` prefix) → W5.4 decommission (`docker rm` the 2 containers + `docker volume rm hub-substrate-data` = the irreversible step; images left, cosmetic). **Architect-tightened: TWO hard gates before the volume-rm** — (1) local integrity cross-check of the fresh dump vs `cutover/hub-cutover-20260520-224232.dump` (expect 18541 entities; mismatch → STOP+surface), (2) post-upload GCS-object-verified-present+intact (not just "local dump good" — guards the silently-failed-upload data-loss path). **W5.5 archive DONE 2026-05-21 — BOTH GATES GREEN.** Gate 1: entity-count 18541 (exact), dump size byte-exact vs cutover dump (8,772,560 B), `pg_restore --list` valid (46 TOC entries, `entities`+`bearer_tokens`). Gate 2: uploaded → `gs://…/archive/mission-86-final-local-hub-state-20260521-042041.dump` (+`.meta`); GCS object verified landed — size + crc32c (`FsyVvQ==`) + md5 (`7rKxucpWlV4NBu6CSp1tZg==`) all match local. The local Hub's final state is permanently + hash-verified preserved. **W5.4 GO'd 2026-05-21 — Director-approved.** Backup posture independently verified by architect (gsutil): local-Hub final state in 2 independent GCS objects (`archive/` + byte-identical `cutover/hub-cutover-20260520-224232.dump`); cloud Hub on 26 hourly `snapshots/`. greg executing W5.4 (`docker rm` ×2 containers → `docker volume rm hub-substrate-data`, images left). Then: work-trace W5.4/W5.5 + AG-W5.8 → `agent-greg/mission-86-w5-close` branch → W5-close PR → architect cross-approve+merge → Phase 9 close + Phase 10 retro. GCS no-expiry retention-config = operator/Director close-time follow-up.
  - Remaining W5 work-trace entries (decommission/close) → a small final W5-close PR.
  - **Operator tooling note:** `get-agents.sh` was failing against the cloud Hub — `~/.config/apnex-agents/director.env` carried the stale local-Hub token (`9dtfAY…`), not the cloud token. Architect updated director.env → get-agents.sh works (confirmed both agents on `claude-0.1.4` / commit `359738f`). The operator-CLI cred files (`~/.config/apnex-agents/*.env`) were NOT migrated at the W4 cutover step-3 (only adapter configs were) — Phase-10 cutover-runbook gap.
  - **Cloud-Hub image — CORRECTED (greg digest-verified):** `ois-hub-prod` = `sha256:1f8655a5…` = `f35b08a` = #224 — **current as of W4, NOT stale.** It carries the bug-103 Hub-half; AG-W5.9's Hub-side dependency is satisfied, no redeploy. The earlier "stale/frozen image" framing was an architect assert-from-incomplete-evidence error (corrected to Director). `list_available_peers`/`list_messages` `-32602` is a non-blocking tool-registration nuance, not vintage → idea-306 curiosity.
  - **AG-W5.9 GATE — plugin re-release (surfaced to Director 2026-05-21):** AG-W5.9 (+ mission-close AG-W5.8) needs the bug-103 *adapter-half* (`firstTimerEnabled` + reconnect-hook + bug-106 cog-layer fix, merged #224) live in running adapters. Delivery vehicle = an `m-github-releases-plugin-distribution` release (`.github/workflows/release-plugin.yml`, tag-push `v*` triggered). **Only release ever cut = `v0.0.0-test` (May 8 dry-run); no real release exists.** Architect recommendation to Director: cut the first real release `v0.1.4` via the pipeline during the soak window; running adapters reinstall (operator-assisted). AG-W3.12 (harness pre-merge form) verified at #224 — closed.

## Sequence — the architect's immediate path
1.–6. ~~`gsutil` fix → rehearsal → #227 → #3-go → cutover SUCCEEDED → step-3 token fix → W4 COMPLETE → PR #228 (W4-closeout) cross-approved + merged.~~ **ALL DONE.**
7. **← FRESH LILY RESUMES HERE.** mission-86 engineering COMPLETE — W5.4 decommission executed, W5-close PR #231 merged → `main @ 579dace`, AG-W5.8 close-ready. **Architect action: drive Phase 9 close + Phase 10 retrospective with the Director (gate-point, surfaced 2026-05-21).** The retrospective = a bilateral calibration pass (material logged below) + mission-status close + follow-on shaping (idea-306 Rocky re-platform, idea-307 fully-native plugin install, the 2 close-time follow-ups). thread-603 stays open for the close/retro coordination — converge it once the retrospective lands. Mission-engagement is otherwise done.
   - apnex-org PR-flow: `gh pr review --approve` + `gh pr merge --squash --admin`; required CI gates = `test`, `vitest (hub)`, `workflow-test-coverage`, `no-engineer-id`, `secret-scan`; the 4 non-hub `vitest` cells are documented tarball-dep debt — non-blocking (`mergeStateStatus: UNSTABLE` is the expected merge-ready state).

## Director-approved decisions
- **Continue on COS** — ship mission-86 on COS; re-platform the Hub VM to a flexible OS (Rocky) as a follow-on — **idea-306** lodged. NOT a mid-W4 reset. (Rationale: COS's hardened immutability caused recurring friction — F12 noexec /var, bug-107 read-only /root, the `gsutil`-not-on-COS finding — and forbids root/kernel changes the Director wants; F15's Cloud NAT dissolved the original COS premise.)
- **Design v2.10** — W5 trimmed of COS-specific validation.
- **Attempt-#3 gating = Option 1 (Director-approved 2026-05-20):** after the `gsutil` fix + the RESTORE-rehearsal pass, the architect brings the Director a *light* "ready for #3 — go?" re-confirm (a post-incident re-acknowledgment). This supersedes, for attempt #3, the earlier operator directive that the cutover runs engineer+architect-driven with the Director re-engaging only at the client-restart. **→ The fresh lily MUST relay Option 1 to greg on thread-601 when the fix+rehearsal are done — greg is currently holding the earlier directive.**

## Open bugs
- **bug-103 + bug-106** — `investigating`; resolve at **AG-W5.9** (the W5 production end-to-end proof). Needs a plugin re-release (`m-github-releases-plugin-distribution`) to deploy the bug-103 adapter-half — surface the release timing at W5-planning.
- **bug-104** — resolved (#224). **bug-101, bug-102** — resolved.
- **bug-105** — `scripts/local/start-hub.sh` missing `--network` — minor; open; W5-or-follow-on.
- **bug-107** — Watchtower AR-auth (COS `DOCKER_CONFIG` path) — `investigating`; NOT fixed on COS; resolved by idea-306 (Rocky's writable /root).

## Follow-on ideas filed
- **idea-307** — Fully-native Claude-plugin install (bundle the shim → pure marketplace plugin, drop `install.sh` + `.tgz` channel) — filed 2026-05-21, Director-requested follow-on surfaced at W5; needs full Survey + Design (NOT a triage-skip).
  - **idea-307 UPDATED 2026-05-21** (`update_idea`, on adapter reconnect) — folded in the version-skew sub-finding (static `plugin.json` `version: 1.0.0` breaks native update-detection + is the root of the `install.sh` cache-stomp hack; fix = bump `plugin.json` per release synced with `package.json`) + the /tmp-install-footgun operational note from the incident.

## Phase 10 calibration material — RETRO COMPLETE 2026-05-21
**Retro walked live with the Director. Dispositions: #79 ← assert-from-incomplete-evidence (FILED) · #80 ← dry-run-doesn't-exercise-the-remote-surface (FILED) · #81 ← operator-credential-files-not-migrated (FILED) · #82 ← constrained-platform-toolchain-reactive (FILED) · `/tmp`-install footgun → FOLDED into idea-307 (no standalone calibration). Candidates as originally logged below.**
- **Architect assert-from-incomplete-evidence** — recurred: the original bug-103 mis-filing, the v2.0 self-message-skip frame, the W4 "cutover didn't reach DRAIN" inverted read, and the W5 "cloud Hub image is stale/frozen" claim (disproved by greg's digest-check — `f35b08a` is current-as-of-W4). Architect spec-level inferences on infra/substrate work need code/evidence-verification before assertion.
- **Dry-run-prints-but-doesn't-execute-the-remote-surface** — TWO W4 cutover attempts failed on execution-time defects (`confirm()`/stdin, `gsutil`-on-COS) that the bilateral audit + the dry-run both missed because the dry-run only *printed* the remote commands. Audits of operational scripts must exercise the remote/privileged surface, not just its printed form (#62-class wire-flow gap).
- **Cascading-adjacent-defects on infra missions** — bug-103→bug-104→bug-106; the F-series findings; the COS-isms. Infra/substrate missions surface layered unknowns — budget for it (spike wave / validate-first conditionals).
- **Operator-credential-files-not-migrated** (engineer-surfaced 2026-05-21) — the W4 cutover migrated Hub *state* but not the operator-machine credential/token artifacts (`.ois/adapter-config.json` hubToken — W4 step-3 gap, fixed #228; `~/.config/apnex-agents/*.env` HUB_API_TOKEN — surfaced when `get-agents.sh` failed). Lesson: a state-cutover checklist must enumerate operator-side credential/config artifacts, not just the DB.
- **`/tmp`-install footgun** (engineer-surfaced 2026-05-21) — the v0.1.4 plugin install run from a `/tmp` extract dir → marketplace source captured a non-persistent path → reaped → adapters failed to load post-restart. Install-target durability belongs explicit in operator reinstall steps (folded into idea-307's operational note).
- *(greg's engineer-side reads on the above two + the dry-run lesson posted on thread-603 2026-05-21 — inputs for the bilateral pass, not final calibrations.)*

## Director-relationship notes
- Director engages at gate-points and has engaged actively on mission-86 (W4, COS-vs-Rocky, the gate questions). Surface decisions as **prose with the architect's lean** — the Director rejected a formal multiple-choice prompt once and asked for a prose recommendation.
- `vertex-cloudrun` + `director-chat` are deprecated (Director-direct) — a new UI solution is coming; don't invest in either.
- The cutover is engineer+architect-driven; the Director (operator) is hands-on for step 3 (the adapter `OIS_HUB_URL` flip + both session restarts — a session can't restart itself).

## Engineer (greg) context
greg has been context-cleared + re-onboarded twice this mission (at W3→bug-103, and bug-103→W4) — the current greg is the W4 session. He surfaces impasses well (the `--yes` and `gsutil` findings) and stops-rather-than-iterate-fixes. Coordinate via thread-601.
