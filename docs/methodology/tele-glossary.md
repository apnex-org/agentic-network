# tele-glossary.md — TOMBSTONE (mission-103 constitutional transition)

**Status: RETIRED.** This glossary — the decoder from teleological-goal id to short-name to mandate — is retired along with the tele layer it decoded.

## The decoder now

The constitutional decoder is the **mission-kit axioms** themselves. Read them via the Hub's constitutional surface:

- `get_axiom` / `list_axioms` — the axioms `A0`–`A14`, each carrying its own title, mandate, mechanics, faults, success signals, and **Provenance** (which records the teleological goal it crystallized). The axiom surface *is* the decoder.
- `get_charter` — the OrgCharter: the org's specific mandates + its axiom bindings with provenance.

Retained as a tombstone so inbound links resolve; it carries no live tele semantics and exposes no verbs.
