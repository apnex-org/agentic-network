# M-Adapter-Modernization — Brainstorm (DRAFT, accumulating)

**Status:** brainstorm-in-progress (Director priority, post-stint-6-SR, 2026-06-29). Multi-session "figure it out" BEFORE a formal Idea→Design. **Scope: Adapters FIRST, then Agent harnesses. Hub + Transport-protocol PARKED.**

**Origin:** the stint-6 strategic-review under-represented infra/deployment (candidate_A = the adapter membrane; deployment/containerisation never summited = a backlog gap). Director prioritized adapter-modernization directly. Live pain this session: the proxy *wedged* (Hub-session dropped, no auto-reconnect → manual kill+restart) + bug-203 tool-surface staleness + directory-source provenance-impurity (`sdkDirty`).

## North-star (Director, 2026-06-29) — ALL THREE, not a trade-off
1. **Pure resilience** — self-healing adapter; no silent wedge; no manual kill+restart; survives Hub blips/redeploys by construction. [tele-7 Resilient Ops · tele-9 Chaos-Validated]
2. **Clean reproducible distribution** — versioned / npm-published / reproducible installs; no directory-source / hand-rebuild / `sdkDirty`; provenance-honest. [tele-2 Isomorphic · tele-1 State-Transparency]
3. **Maximal shared core regardless of harness** — ONE adapter brain; per-harness pieces are thin glue. [tele-3 Sovereign Composition]

## Core invariant — THE thesis (Director, 2026-06-29)
**Uniform transport from the adapter toward the network; per-harness variation confined to the LAST HOP.** Everything from the kernel *outward to the Hub* — the resilient channel (L1 reconnect), the push protocol, ack/retry/ordering, the typed coordination entities — is IDENTICAL for every harness. The only thing a harness changes is *how the already-delivered message makes its final native landing*. That membrane is what makes "maximal shared core regardless of harness" **literally** true. Director's words: *"the transport is uniform leaving the adapter towards the network — that is the whole point."*
- **Consequence:** the architecture is ROBUST to not-yet-knowing a harness's exact injection mechanism — the variation is fully contained in the last hop, so the kernel→network spine is locked regardless. The claude pilot confirms the landing detail empirically + slots it into the capability matrix; it does NOT block the spine.

## Target architecture (implied) — thin-shim / fat-resilient-kernel / npm-distributed
- **`@apnex/network-adapter` = fat shared kernel:** ALL MCP↔Hub logic + the connection lifecycle (establish / heartbeat / **auto-reconnect** / self-heal) + version-honesty. Resilience lives here, ONCE, for every harness.
- **`@apnex/claude-plugin` / `@apnex/opencode-plugin` = THIN shims:** only the harness-specific binding (how the host mounts MCP tools + spawns the runtime). No brains. A new harness (cursor / a CLI / …) = a new thin shim over the same kernel — zero kernel duplication.
- **Distribution:** npm, pinned + reproducible (shim depends on a pinned kernel version; lockfile); provenance-honest (running version = kernel-sha + shim-sha, truthfully surfaced); directory-source becomes a **dev-only** affordance, never prod.

## Key tension — "pure resilience" has TWO flavors that split on solvability
- **Connection-resilience** (this session's wedge: the proxy's Hub-session dropped + never reconnected) → **KERNEL-SOLVABLE.** Robust auto-reconnect / backoff / self-heal in the shared kernel; transport-protocol-agnostic (doesn't touch the parked transport theme).
- **Tool-surface-resilience** (bug-203: claude-code won't re-enumerate the tool-surface mid-session; opencode does) → **HOST-LIMITED, not fully kernel-solvable.** Even a perfect kernel can't make claude-code re-read tools without a restart. Needs either a clean/scripted restart OR idea-391 (system CLI→Hub REST, sidestepping the MCP tool-surface). The one place the north-star hits a host wall.

## Open forks (to brainstorm)
- **(i) shim↔kernel boundary** — minimal shim vs maximal kernel; makes "maximal shared core" precise. [the SPINE — likely first]
- **(ii) resilience contract** — what auto-reconnect / self-heal / version-honesty the kernel guarantees + how it handles the bug-203 tool-surface wall.
- **(iii) distribution + migration** — the npm-pinned model + how we move off directory-source without disruption.
- **framing Q:** is idea-391 (CLI→Hub REST) in-scope for this, or do we design resilience *around* the MCP host-limit and treat 391 separately?

## Decision log
- **2026-06-29 (Director) — tool-surface-resilience is solved at the HARNESS layer (containerisation + restarts), NOT the adapter (no hot-reload, no idea-391 on the critical path).** Mechanism: a tool-surface change → restart the containerised harness → the STARTUP-bootstrap path (which DOES honor the live surface — the path that worked today; bug-203 only blocks the *mid-session* re-enumerate) re-enumerates fresh. Containerisation makes the restart cheap/clean/reproducible + auto-fresh caches (no stale `.ois/tool-catalog.json` / plugin-cache → the manual clear+restart dance becomes automatic).
  - **Clean separation of concerns:** ADAPTER (priority 1) owns CONNECTION-resilience ONLY (auto-reconnect/self-heal on a live session); HARNESS-container (priority 2) owns TOOL-SURFACE-resilience (restart re-bootstraps) + reproducibility + cache-cleanliness.
  - **Harness-containerisation = triple win:** (1) tool-surface re-bootstrap, (2) cache-cleanliness (fresh container = clean slate), (3) provenance-purity (reproducible image kills `sdkDirty`).
  - **idea-391 (CLI→Hub REST) DE-SCOPED** from tool-surface-resilience → separate/optional track, not a dependency.
  - **Dependency: harness-restarts ⟂ cold-pickup quality.** A restart drops in-session context → the agent re-hydrates from durable state (memory-anchor pattern). VALIDATED this session: the SR run survived 2 restarts + a Hub-disconnect via cold-pickup. The restart-strategy rests on this substrate (already solid).
  - **Adapter resilience scope NARROWS to connection-resilience** → simpler adapter design (no hot-reload).

- **2026-06-29 (Director) — direction: CONTAINERISATION; claude-code CLI FIRST.** Containerisation SUBSUMES the distribution/update option-axes — the harness IMAGE becomes the single unit for distribution + update + tool-surface-refresh + reproducibility + cache-reset; **npm is just the SOURCE baked in.** So the big forks (npm vs marketplace vs dir-source; reinstall vs rebuild vs hot-reload) are RESOLVED by the container direction; only narrow instance-decisions remain.
  - **PLAN — design-for-both, build-claude-first:**
    - UP-FRONT DESIGN (generalizable spine, designed for BOTH harnesses so opencode is a drop-in — protects maximal-shared-core): kernel/shim split + npm packaging + a UNIFORM register-SPI + the image spec.
    - PHASE 1 — claude-code CLI container PILOT (worst-pain harness: bug-203 + the wedge). Acceptance: auto-reconnect (no silent wedge) · restart re-bootstraps tool-surface · reproducible image (no `sdkDirty`) · version-honest.
    - PHASE 2 — opencode drop-in (2nd thin shim over the same kernel + register-SPI) → PROVES uniformity + shared-core.
    - PHASE 3 — automate (CI publish npm + build image; watchtower-pull + restart).
  - **Narrow remaining decisions (claude-first):** (i) claude's register mechanism in-image (the (a)/(b), scoped to claude); (ii) image contents (claude-code + adapter + git + creds + repo-access); (iii) ephemerality model (disposable container; state via Hub+git+memory cold-pickup — PROVEN this session); (iv) update-trigger (watchtower-pull vs CI-push vs manual-first).
  - **METHODOLOGY:** graduate this brainstorm → a formal Idea (anchors the infra backlog-gap the SR flagged) + a Design (this doc), claude-code pilot = first mission-slice.

- **2026-06-29 (Director) — harness scope: design target = claude + opencode NOW; antigravity / pi / hermes = future drop-ins.** Sharpens "design-for-both" → **design-for-claude+opencode, build-claude-first.** Rationale: two harnesses that genuinely differ (Claude has no model-end hook; opencode downgrades the system prompt into AGENTS.md — both in scion's capability matrix) JUSTIFY building the **3-valued capability matrix + per-harness dialect** now (not YAGNI). The later three validate the shim is thin (the generalization test) but are NOT built now. opencode is the Phase-2 drop-in that PROVES maximal-shared-core.

- **2026-06-29 (Director) — resilience authority: MULTILAYER, separated by object-of-protection; lease STAYS; do NOT design all interlock up front.** Each layer protects a different object at a different altitude → they can't fight → no big interlock design needed.
  - **Layer 1 — kernel (in-container): protects the CONNECTION.** Actor = kernel auto-reconnect/backoff/self-heal. Fires when the Hub push-channel drops but the process is alive (this session's wedge). The *new* build; the **claude-pilot acceptance criterion**.
  - **Layer 2 — platform (outside-container): protects the PROCESS/CONTAINER.** Actor = k8s RestartPolicy / systemd / watchtower restart. Fires when the kernel or container *dies* (kernel can't restart itself if PID-1 is dead). Pilot = whatever the runtime gives cheaply (local/VM → systemd/watchtower); k8s-grade later.
  - **Layer 3 — Hub lease-expiry (Hub-side, ALREADY BUILT): protects the WORK.** Actor = sweeper reclaims item → `ready`. The ONLY layer that catches the **live-but-wedged cognitive stall** (process healthy by every OS check, just not progressing) — no process-supervisor can see that, only the work-graph can. L1/L2 protect *liveness*; L3 protects *progress* → not redundant. Director: "I like our lease system, I think it's genuinely useful." STAYS.
  - **Why no up-front interlock:** the layers NEST naturally — kernel inner-loop exhausts → process exits → platform outer-loop restarts → re-hydrate (cold-pickup) → re-claim the work L3 already freed. The ONLY seam (`restart → re-hydrate → re-claim`) is **already proven** (SR run survived 2 restarts + a Hub-disconnect this session; re-claim re-binds byte-stable). Nothing new to design at the seam.
  - **The discipline that makes multilayer REAL (scion lesson):** scion had a heartbeat but nothing acted on it (report-only) + no lease (cooperative, no reclaim) = telemetry without authority. **Rule: every layer has an ACTOR that acts on its signal, not just a reporter.** kernel reconnects / platform restarts / sweeper reclaims = three actors, three altitudes. Difference between three dashboards and actual resilience.
  - **Pilot consequence:** leans on two EXISTING layers (L3 lease + whatever L2 the runtime gives) and builds ONE new (L1 kernel reconnect). Closes the scope further → simpler pilot.
  - **⚠ CORRECTED by shim-audit (2026-06-29):** L1 kernel-reconnect ALREADY EXISTS (forever-backoff `reconnectWire`, re-handshake-on-reconnect, PollBackstop, heartbeat). The lived wedge is most plausibly a **stale-deployed-build / provenance** problem, NOT missing reconnect code. So the pilot's L1 work reframes from "build reconnect" → **"harden + chaos-validate + provenance-GUARANTEE the existing L1"** + add the genuinely-missing **consumption-ACK/ordered-queue on injection** + the L2 supervisor. The PRIMARY fix for the wedge is **reproducibility (pillar 2) fixing the pillar-1 symptom.** See `m-adapter-modernization-shim-audit.md`.

- **2026-06-29 (Director) — package topology + the pull/push split + registration vs marketplace.** Settled the "do we need a new package, and how do tools+injection actually attach" question.
  - **TWO LAYERS, ONE PACKAGE PER HARNESS** (NOT three; no new sibling package): `@apnex/network-adapter` (shared kernel = brains) + one per-harness package (the existing `@apnex/<x>-plugin`; rename to `harness-<x>` is cosmetic/optional — see below). The per-harness package carries up to THREE elements: (1) **manifest** (data: command/env/capability/auth-order/injection-flags/dialect); (2) **kernel-loaded hooks** (code, optional: `translateConfig`/`parseStatus`); (3) **a literal native injection-plugin** (code, host-loaded — CONDITIONAL per harness, see pull/push).
  - **Host-binding is TWO surfaces, only one dissolves:** **(a) tool-surface = PULL** — MCP server, the harness *calls* our tools → dissolves into kernel-emitted native config. **(b) injection = PUSH** (our differentiator) — MCP CANNOT do it (it exposes callable tools, can't shove a message into the live turn); needs a harness-INTERNAL hook → does NOT dissolve. So my earlier "host-binding dissolves into config" was too strong (Director correction). The name `*-plugin` is defensible *because* a literal plugin genuinely lives in there (opencode).
  - **Last-hop landing differs per harness; the kernel→network transport does NOT (the core invariant):** opencode = literal plugin hooking `injectLLM` (needs the native plugin); claude = (Director's read, pilot-confirm) MCP-SSE channel + a runtime flag enabling push-notifications → may need NO separate native plugin (injection rides the kernel-as-MCP-server SSE channel). Captured as a capability-matrix field `injection-mechanism: { plugin-hook | SSE-push+flag | tmux-fallback }`. The injection-plugin (element 3) is therefore the heavier/OPTIONAL case, not universal.
  - **Kernel stays fat / shim stays thin even for injection:** kernel holds the resilient Hub channel + ack/retry/ordering (L1); the per-harness landing (plugin or SSE-flag) is the THIN final hop that takes the kernel-delivered message → native inject point.
  - **Registration SHAPE: YES · Marketplace DISTRIBUTION: NO (bake instead).** We conform to each harness's native plugin registration contract so its loader mounts our injection hook (unavoidable — it's how push lands). But we get the plugin IN by BAKING it into the image at the loader's path (an image layer), NOT `install-from-marketplace` (claude marketplace / opencode registry) at boot. Why: reproducibility (marketplace = the exact `latest`-float the scion audit told us to kill), resilience (no boot-network failure mode), uniformity (one bake mechanism vs N marketplaces), control (else we'd publish to a PUBLIC marketplace just to self-install — a Director-gated line). **npm = source · bake = distribution · harness loader = registration contract.** (Distribution to OUTSIDE users = separate, later, Director-gated.)
  - **Pilot lead-unknown:** claude-code's injection landing-point — confirm the SSE-channel+runtime-flag mechanism (vs needing a literal plugin vs tmux-fallback). First thing the claude pilot nails; adjacent to bug-203. Robust either way (last-hop-contained).
  - **GROUND-TRUTH:** the per-harness shims ALREADY EXIST (claude + opencode), delivering last-mile integration today (Director, 2026-06-29). So this is EVOLVE-existing, not greenfield-shim — the modernization is the KERNEL (L1 resilience, uniform transport, containerised npm-baked distribution) + refactoring the existing shims to the manifest+hooks+injection-plugin shape. Shim-audit COMPLETE (2026-06-29) → see `## Shim-audit ground-truth` below + full `m-adapter-modernization-shim-audit.md`.

- **2026-06-29 (Director) — #6 runtime target: docker (docker-compose) FIRST; k8s LATER, not now.** The pilot deploys onto docker-compose. Rationale + ties:
  - **scion cautionary tale:** don't expose a half-built host-type (their CloudRunRuntime is an unimplemented stub). COMMIT to docker fully before claiming k8s.
  - **Closes the L2 resilience layer for the pilot:** docker restart-policy (`restart: unless-stopped`) = platform-supervision; optionally **watchtower-pull** for image-update — the SAME mechanism as our prod Hub deploy (watchtower auto-pulls hub:latest → restart). Continuity: agent containers reuse the proven watchtower-pull update path.
  - **docker-compose = local-dev + pilot deployment in ONE** (one compose file spins kernel+harness containers); k8s/cloudrun are later host-types behind the same narrow Runtime seam, built only when fully implementable.

- **2026-06-29 (Director) — #7 telemetry: NOT BUILT (verified) → transport-decision DEFERS to the telemetry mission.** Repo grep confirms: no `@opentelemetry` dep (only transitive lockfile mentions), no telemetry/observability source module; `get_metrics`/`get_backlog_health` surface work-graph STATE (counts/backlog-health = substrate introspection), NOT a `gen_ai.*` observability pipeline. Telemetry is greenfield = candidate_E / idea-343; bug-194 = the live silent-failure gap.
  - **Deferred-with-lean:** when we build it (idea-343), the scion data-driven **dialect → ONE OTEL `gen_ai.*` pipeline** pattern is the reference (new harness = a dialect file). The same-vs-separate-channel transport question gets decided THEN; recorded **lean = SEPARATE channel** (our own memory warns against piggybacking coordination on a lossy notification pipeline; scion separates structured hook-events from the PTY stream). Not in this brainstorm's scope (adapters/harness-containerisation first).

- **2026-06-29 (Director) — #4 secret transport RESOLVED for the pilot: copy the existing flat credential files in; harness self-refreshes; no rotation handling.** Ground-truthed against the dev machine — both LLM creds are single `0600` flat files (no keychain): claude `~/.claude/.credentials.json`, opencode `~/.local/share/opencode/auth.json`.
  - **CORRECTION to the earlier #4 framing:** LLM auth is the HARNESS's own concern (confirmed by shim-audit) — the **harness self-refreshes** its access token against the provider. The **kernel does NOT refresh LLM creds** (the kernel-owned refresh loop is for the Tier-0 Hub token only). The container's only job for LLM creds = place the credential file at the harness's expected path at boot.
  - **Cred model (Director-SETTLED 2026-06-29; artifact REFINED by claude-code-guide check):** the credential is **constructed ONCE on the host** and injected into each claude container ("construct once on host, inject into the container"). **Artifact = `claude setup-token` → a one-year OAuth token → injected as `CLAUDE_CODE_OAUTH_TOKEN`** (env var or mounted secret) — NOT a copy of `.credentials.json`.
    - **Why the artifact changed (claude-code-guide check, 2026-06-29):** `CLAUDE_CODE_OAUTH_TOKEN` is the *documented, supported* headless/container subscription-auth path; `.credentials.json` self-refresh is **UNDOCUMENTED** for subscription OAuth → relying on it inside a container is a quiet risk. The one-year token sidesteps refresh entirely (valid 1yr; re-mint once a year). Same construct-once-on-host model, more robust artifact (a single token, not the full credential blob).
    - **Inject at RUNTIME — env var or mounted secret — NEVER baked into the image** (keeps the image cred-free + reproducible, pillar-2; pushable without leaking the subscription). One harness per container → a claude container carries only its token; an opencode container carries only its own `auth.json` (runtime-injected, not baked).
    - **Broker-minted-ephemeral is FEASIBLE + DOCUMENTED via `apiKeyHelper`** — a script claude calls on-demand / on HTTP-401 to fetch a fresh token (TTL via `CLAUDE_CODE_API_KEY_HELPER_TTL_MS`). So the more-secure multi-host end-state (durable secret never enters the container) is **confirmed open, not blocked** — DEFERRED to multi-host (the pilot uses the one-year token; not needed on the Director's own box).
    - **Distinct auth modes (confirmed):** subscription OAuth (`CLAUDE_CODE_OAUTH_TOKEN`) ≠ `ANTHROPIC_API_KEY` (metered *console* key, separate billing — NOT the subscription) ≠ `ANTHROPIC_AUTH_TOKEN` (bearer, LLM-gateway pass-through, no refresh). `--bare` mode skips OAuth/keychain. No official Dockerfile/devcontainer guidance exists (we author our own).
  - **Escape hatch (only if Anthropic ever rotates the refresh token):** swap copy → persistent volume (one compose line). Not pre-built.
  - **Tiers (pilot):** Tier-0 Hub token = mounted file (already works via `.ois/adapter-config.json` / `OIS_HUB_TOKEN`) · LLM creds = copied flat files (above) · git creds = mounted token/key (pilot) · Tier-2 prod-cloud = secret-manager, never Hub. The Hub-as-cred-broker / pull-over-channel option is DEFERRED (OAuth self-refreshes → low value; revisit at multi-host).
  - **Flags (non-blocking):** the injected token = the Director's account subscription (every container auths as him — fine for pilot; watch concurrency). Construct-on-host = dev-expedient; reproducible multi-host end-state = provisioning-helper → secret-store / `apiKeyHelper` broker (deferred, not pilot).

- **2026-06-29 (Director) — config standardisation: schema-validated JSON manifest (harness STANDARD) + ENV (per-agent INSTANCE), mirroring scion's split.** scion is two-layer, NOT all-env: a per-harness **YAML manifest** (`harnesses/*/config.yaml` — command flags, `env_template`, capability-matrix, auth tables, autodetect, no_auth) = the standardised harness-TYPE; **`SCION_*` env vars** = per-agent instance (identity/runtime); the YAML's `env_template` **binds** them (so it's "YAML defines the contract, env fills per-agent values," not YAML-vs-env). Maps 1:1 onto our manifest + ENV-injection.
  - **OUR format choice: schema-validated JSON manifest (NOT YAML).** Kernel is JS/TS and the manifest ships in the same npm package as the typed hooks → JSON (validated via Zod/JSON-schema at load) gives data-purity + compile/runtime validation of the capability-matrix + `injection-mechanism` enum + native parsing (no YAML dep) + co-location with the typed hooks. YAML reserved only for a hypothetical non-JS harness authoring manifests without our toolchain (not claude+opencode).
  - **Per-agent ENV contract** = identity (agent name/role) + Hub URL + `OIS_HUB_TOKEN` + git creds + worktree/branch + model/quota + **`CLAUDE_CODE_OAUTH_TOKEN`** (the LLM cred is itself an env/secret → slots straight into this contract; reinforces "env for per-agent instance values"). The manifest's `env_template` declares WHICH vars each harness expects; ENV fills them per-agent.

## Design-review outcome + GRADUATION CHECKLIST (adversarial review w6e5e0dqp, 2026-06-29)
6-dimension adversarial design-review (32 findings raised → **15 confirmed** after skeptical verification; 40 agents, 2.26M tokens). **Verdict: READY-WITH-FIXES.** NO architecture-killer — the core (fat-kernel/thin-shim, uniform-transport/last-hop, npm-bake, manifest+ENV, multilayer resilience, docker-first) all SURVIVED and carries into the formal Design intact. What review caught = a cluster of **over-confident closures** that would propagate into the Design as FALSE PREMISES → reframe at graduation, do NOT inherit.

### MUST-FIX before graduation (3 — reframe the over-confident closures)
1. **Reopen the resilience "nest" (decision-log line "Why no up-front interlock") — the live-but-wedged container has NO restart-actor** (the exact failure that motivated the effort). Forever-backoff → L1 never exhausts → process never exits → docker L2 restart-on-exit never fires; L3 reclaims WORK only, not the container. The **keepalives-flowing-but-session-dead** edge slips BOTH L1 (kernel believes connected) + L2 (process alive) → a silent zombie holding the OAuth token. FIX: add a **liveness self-watchdog** (app-level session probe fails → PID-1 self-exit → L2 restart) OR explicit wedge-DETECTION → degraded state in `get_agents`. Separately DEMOTE the "cognitive re-hydration already solid" claim (byte-stable = only the MECHANICAL re-claim; unvalidated for involuntary restart mid-long-cognitive-node). This is scion open_question #1 (self-heal ↔ lease interaction) — declared closed when OPEN.
2. **Resolve the kernel-topology ↔ boot-model contradiction.** Doc's boot order (kernel connects THEN launches CLI; kernel-as-PID-1) is IMPOSSIBLE — ground truth: the CLI SPAWNS/LOADS the kernel (child, not PID-1). FIX: pick **EMBEDDED** (PID-1 = thin supervisor; CLI spawns the kernel-shim; injection in-process — matches reality, RECOMMENDED) or **SPLIT** (standalone kernel + a fully-specified IPC seam). L1/L2 nesting is topology-independent → clean either/or.
3. **Confirm the wedge root-cause before anchoring "reproducibility is the PRIMARY fix."** It's a hedged inference ("most plausibly" stale build); the competing keepalives edge (= #1) would NOT be fixed by reproducibility. FIX: soften the framing; add a diagnose-first/provenance-confirm GATE; chaos-test the named edge (session dead while keepalive flows); tie ONE acceptance criterion to the lived incident. (Do NOT add a hard "reproduce the historical prod wedge" blocker — chaos + L2/L3 is right-weight.)

**Convergence signal:** mustFix #1 + #3 point at the SAME soft spot (the undetected-dead-session live-wedge) across independent lenses — the one genuine hole in an otherwise-sound resilience story.

### SHOULD-ADDRESS (fold into the formal Design; non-blocking)
- **opencode auth-parity** — reverts to the self-refresh pattern rejected for claude; mark a KNOWN Phase-2 gap; treat LLM-auth as a per-harness capability-matrix entry (NOT a uniform ENV slot); record the shared-Director-token fleet-concurrency cap + the per-account/broker exit.
- **Harden Phase-1 acceptance into falsifiable tests** — add an injection consumption-ACK criterion; "reproducible" = byte-reproducible/digest-pin (≠ "no `sdkDirty`"); "version-honest" is already BUILT (regression-guard, not deliverable); spell out an L1-isolating chaos matrix + a claude-headless-auth run-gate.
- **Caret-vs-exact-pin reproducibility gap** — the publish path rewrites cross-@apnex deps to `^X.Y.Z` (floats); specify the byte-reproducible bake (PREFERRED: bake from monorepo source at a git SHA + `npm ci` + digest-pin bases; fix the "pinned kernel version" wording).
- **Safe-update contract** — quiesce/drain signal + staggered/rolling restart + boot-smoke-gate + rollback-to-prior-`:sha`; keep manual-first as the pilot default; drop the watchtower-`:latest`-vs-`:sha` "contradiction" (dual-tag already reconciles).
- **Sequence the pilot reproducibility-FIRST** — the reproducible+provenance-guaranteed image is the FIRST milestone (can't chaos-validate a build you can't guarantee-deploy); manifest-extraction is a parallel refactor; injection-ACK is the last/riskiest net-new item.
- **Disambiguate the two acks** — Hub-leg ack/retry/ordering (kernel↔Hub, UNIFORM, already implemented = what delivers "survive blips by construction") vs last-hop CONSUMPTION-ack (per-harness: opencode awaitable `promptAsync` = yes; claude one-way MCP notification = partial/best-effort + backstop chain). Add an `injection-ack` capability-matrix cell.

**These fixes are the entry-gate to the formal Idea+Design.** Full review: workflow runId `w6e5e0dqp`.

## Shim-audit ground-truth (a06e7b9, 2026-06-29) — what EXISTS, and how it corrects the design
Full: `m-adapter-modernization-shim-audit.md`. The load-bearing corrections:
- **Topology reality:** 3 shared kernel-packages (`@apnex/network-adapter` v0.1.4 + `@apnex/message-router` v0.1.2 + `@apnex/cognitive-layer` v0.1.2) + 2 shims (`@apnex/claude-plugin` v0.1.9, `@apnex/opencode-plugin` v0.2.1). The "2-layer" framing holds CONCEPTUALLY (kernel-layer = the 3 shared); physically 5 packages.
- **Injection CONFIRMED — resolves the pilot lead-unknown:** claude = a custom **MCP server→client notification** `notifications/claude/channel` (NO literal plugin — confirms heavier/optional read; the kernel↔Hub leg is SSE, the adapter→claude landing is the MCP notification). opencode = a **literal `@opencode-ai/plugin`** via SDK **`session.promptAsync`** (`promptLLM` actionable / `injectContext` silent). **Asymmetry:** claude folds tools+injection through ONE MCP channel; opencode SPLITS them (HTTP-MCP tools / SDK-promptAsync injection). Capability-matrix `injection-mechanism` seeds: `{ mcp-server-notification | plugin-hook(promptAsync) | tmux-fallback }`.
- **L1 resilience REFRAME (headline):** auto-reconnect **already exists** (forever-backoff `reconnectWire`, re-handshake-on-reconnect, `PollBackstop`, heartbeat). The lived wedge → most plausibly **stale-deployed-build / provenance**, NOT missing code (stale comments reference a DELETED `McpConnectionManager`; directory-source lags HEAD). **→ The primary fix for the wedge is REPRODUCIBILITY (pillar 2) fixing the pillar-1 symptom — not new reconnect logic.** Remaining genuine L1 gaps: consumption-**ACK on injection** (net-new), chaos-validation, stale-comment cleanup, the L2 supervisor.
- **Creds today:** Hub token only, from `.ois/adapter-config.json` or `OIS_HUB_TOKEN` env. **LLM + git creds are NOT an adapter concern today** → the entire #4 cred pipeline (OAuth-refresh-in-kernel, pull-over-channel) is **NET-NEW** (confirms the #4 discussion is greenfield, not a refactor).
- **Distribution:** directory-source (=`sdkDirty`) in practice; a publish path + version-honesty mechanism (build-info commitSha/dirty → `get_agents`) EXIST but under-exercised; `update-adapter.sh` is **claude-only**. Gap = reproducible-build discipline + deployed-version-guarantee, NOT tooling-from-scratch.
- **bug-203:** REAL + **claude-code-host-specific** (NOT adapter-fixable; the adapter emits `tools/list_changed` correctly, the host ignores it; **opencode honors it**). `ToolSurfaceReconciler` built kernel-side. → VALIDATES the container-restart-re-bootstrap decision.
- **Ranked gaps to close:** (1) **declarative manifest + capability-matrix + dialect** (the one genuinely-missing structural piece — config is hardcoded TS today; everything else is "formalize what's injected"); (2) **distribution/provenance** (the likely true wedge cause); (3) **resilience GUARANTEE not implementation** (chaos-validate + injection-ACK + L2 supervisor); (4) **auth pipeline** (net-new).
- **Net effect on the pilot:** SMALLER + sharper than "build resilience" — the pilot is mostly *(a)* extract the hardcoded shim config → a manifest + capability-matrix, *(b)* make the build reproducible + provenance-guaranteed (kills the wedge + `sdkDirty`), *(c)* chaos-validate the existing L1 + add the injection-ACK, *(d)* containerise (docker-compose) with L2 restart-supervision.

## Prior art — GoogleCloudPlatform/scion (Director-provided, 2026-06-29) — INSPIRATION not verbatim
SCION = experimental multi-agent orchestration testbed for "deep agents" (Claude Code / Gemini CLI / …): **container-per-agent + own git-worktree + own credentials**; runs local / VM / k8s; "less is more" — agents *invoke a CLI tool* + coordinate via NL (group/direct messaging + shared workspace); telemetry from messages + file-access. (scion = "a shoot cut for grafting" — apt.)
- **Differentiator — ⚠ CORRECTED by the deep audit (the pre-audit "scion can't push" hypothesis was WRONG):** scion **DOES** inject inbound AND **DOES** support agent-to-agent messaging (group + direct NL), orchestrator-mediated. Delivery to a recipient = the HOST orchestrator exec-ing `tmux send-keys`/`paste-buffer` into the recipient's live pane (`pkg/agent/manager.go`), NO consumption-ACK, papered with a 300ms double-Enter + 2s debounce. So the differentiator is NOT push-vs-no-push — it is **WHERE + HOW + SEMANTICS**: (1) WHERE — our injector is IN-container (kernel-owned) vs scion's host-EXTERNAL; (2) HOW — ours is an ack'd, ordered channel vs scion's keystroke-simulation-with-timing-hacks; (3) SEMANTICS — ours is typed coordination entities (work/leases/gates/RACI) vs scion's free-text + a closed 5-value status enum (semantics live in the LLM's head). CONSEQUENCE: a host-orchestrator blip WEDGES scion delivery and nothing self-heals; our in-container kernel survives blips by construction. **FRAME: graft scion's container-deployment shell onto our STRUCTURED + RESILIENT + IN-CONTAINER push-core.**
- **One-line framing (Director, 2026-06-29): "we have an adapter-native first-class messaging system, not a keyboard hack."** And the transport choice is the ROOT of the divergence, not a polish: a keystroke channel can only carry *characters* → it structurally **caps coordination at free-text prose** (scion's "less is more / coordinate in NL" is partly TRANSPORT-FORCED, not purely philosophy). A first-class **typed** channel is the **precondition** for our structured work-graph / policy-engine network — typed work-claims / leases / gates / RACI cannot ride a keyboard hack. **The transport sets the coordination ceiling.** This is why the divergence is at the transport layer *beneath* coordination, not at the coordination layer itself.
- **ADOPT (deployment shell):** container-per-agent + worktree + creds · off-the-shelf-harness wrapping · ENV/config-driven boot · multi-runtime (local/VM/k8s) · message/file telemetry.
- **ADAPT:** their host-external keystroke-injection + agent-invoked pull-CLI → our IN-container, kernel-owned, ack'd, typed message-injection (kept alive + resilient in-container by the adapter-kernel, P1). Same push *capability*; we own it inside the container with delivery guarantees + typed semantics.
- **AVOID:** their "less-is-more / models freely coordinate" philosophy — we keep structured roles/RACI/work-queue-FSM/gates/teles.
- **DEEP-READ DONE (8-agent audit w2bjaeug5, 2026-06-29)** → next section. Full structured findings archived at `docs/designs/m-adapter-modernization-scion-audit.json` (headline/adopt/adapt/avoid/per-decision/divergences/open-questions). Decision-areas keyed D-img/D-boot/D-run/D-tmux/D-plugin/D-creds/D-coord.

## scion deep-audit — adopt / adapt / avoid (w2bjaeug5, 2026-06-29) — INSPIRATION not verbatim
8-agent seed-blueprint audit of scion internals (916k tokens; 7 area-auditors + synthesizer). Full JSON: `m-adapter-modernization-scion-audit.json`. The load-bearing distillation:

### The two biggest results
1. **STRONGEST VALIDATION — scion's 3-tier image cascade IS our fat-kernel/thin-shim in Docker layers.** OS+toolchain base (rarely changes) → org layer that multi-stage-BAKES the shared binary + sets a **PID-1 ENTRYPOINT** → a *tiny* per-harness leaf that only installs ONE agent CLI + sets CMD (their gemini leaf = 14 lines; claude leaf = a single `npm install`). One image per harness-TYPE; per-agent diffs (UID/creds/worktree) injected at **runtime, never baked**. This is our agreed architecture, already proven in someone else's Docker layers. **Map our layers straight onto it.**
2. **HIGHEST-VALUE NEGATIVE RESULT — scion BUILT then RIPPED OUT go-plugin RPC harness plugins.** Their verdict (`.design/decoupled-harness-implementation.md`): *"provisioning is a scripting problem, not a systems programming problem"* — go-plugin is "heavyweight… overkill for file-writing provisioning logic." Maps 1:1 to our shim decision: **our per-harness shim must be a declarative manifest + thin module, NEVER process-isolation RPC.** A wrong path we now skip for free.

### The four divergences that CONFIRM our differentiators (scion does the opposite of each pillar)
- **Kernel-owned injection (pillar-1):** scion injection is HOST-EXTERNAL — the orchestrator execs `tmux send-keys` into the live pane per message, **NO consumption ack**, papered with "300ms double-Enter to ensure input is accepted" + 2s debounce (`manager.go:316-321`). Coordination liveness is coupled to orchestrator exec-access → a blip wedges delivery, nothing self-heals. **OUR kernel-in-container owns injection with a real submit/ack + ordered queue → pushed messages survive blips by construction.** Net-new; nothing to reuse.
- **In-system self-heal (pure-resilience):** scion has NONE — k8s `RestartPolicyNever` (`k8s_runtime.go:1240`), no Restart verb (restart = Delete+Run by orchestrator code), heartbeat **reports-only**. **We put resilience INTO the system: kernel self-restart and/or native supervision + lease-expiry reclaim.**
- **Pin-everything reproducibility (no-drift):** scion has the right STRUCTURE (dual-tag :latest + :sha, immutable :sha as BASE_IMAGE, ldflags-stamp, multi-stage, content-hashed bundles) but **floats nearly everything to `latest` — claude has NO pin at all.** A rebuild of the same git SHA can yield different binaries. **Adopt the structure, then PIN EVERY dep + digest-pin bases.**
- **Structured-hook telemetry (not screen-scraping):** scion's telemetry is genuinely strong and reusable — a thin data-driven per-harness **dialect** (YAML event-name + dotted-path) → ONE OTEL `gen_ai.*` pipeline + baked-in field-redaction + file-touch swarm signal. **A new harness = a dialect file, not pipeline code.** (This is our shim/kernel split applied to observability.) Observe via hook-events, **never screen-scrape** the pane.

### ADOPT wholesale (proven, maps clean)
- **tmux model** — mandatory uniform detached session across all runtimes; fixed name; two-window (agent+shell); `new-session -d` then attach; attach/exec ALWAYS as session-owner; harness-exit-to-file wrapper + pane-exited→kill-session teardown; capture-pane as a *snapshot only*; SPDY-API PTY (no kubectl in-image). Treat tmux as **non-durable** (dies with the container; persist in volume/Hub).
- **Narrow Runtime seam** — ~12-verb interface + one config struct + factory(autodetect)+null-object + ALL shared logic in one helper, each backend a thin wrapper.
- **Thin-wrapper-over-declarative-manifest** per-harness adapter + a typed **3-valued capability matrix** (yes/partial/no + reason) so callers gate gracefully (harnesses genuinely differ — e.g. Claude has no model-end hook; OpenCode downgrades the system prompt into AGENTS.md).
- **ENV-injection as the single config contract** (identical across backends); **FAIL-CLOSED boot** (ValidateAuth before launch — serves no-silent-wedge).
- **Harness-agnostic 4-stage cred pipeline** (gather→overlay→resolve→fail-closed-validate) as a shared-kernel resolver; **token-from-FILE not env** (0600 atomic rename, env var deleted) + kernel-owned proactive refresh → long session survives cred churn with no restart.
- **Defense-by-ABSENCE isolation** (UID==host-UID makes chmod worthless); content-addressed config + MANDATORY hash verify; secret materialization at entrypoint (write 0600 BEFORE any process runs).
- **Build provenance** ldflags-stamp (kernel version+SHA self-reported by the running container); dual-tag + immutable-:sha-as-BASE_IMAGE; secret-safe `.dockerignore`; pre-chowned runtime dirs; lean minimal base.

### ADAPT (right shape, our ownership)
- **INVERT the baked-vs-mounted boundary:** scion bakes the CLI but *mounts* the provisioner (redeploy-without-rebuild). **We BAKE the @apnex kernel** (npm-as-source baked, so push-injection survival never depends on a runtime fetch); only per-agent ENV/creds external; update = rebuild + restart.
- **Centralize the pre-start provision contract in the KERNEL** (one impl) instead of N per-harness Python scripts; keep the "pre-start hook prepares native config + selects auth + fails closed" shape, but the shim is a JS/TS module/manifest.
- **Message-injection mechanics** (keep bracketed-paste so TUIs don't eat chars; harness-specific interrupt key) but **our kernel owns delivery** with explicit submit/ack — replacing the external exec + blind-timer debounce.
- **Heartbeat → kernel self-reporting inside a self-healing loop** (detect wedge → reconnect/restart); adopt the **sticky liveness FSM** (waiting_for_input/completed resist clobber by trailing tool events) to fix our known **false-idle reads**.
- **Per-agent worktree over a shared base .git** — but ALWAYS create host-side then mount (NEVER in-container: `--relative-paths` path-identity corruption); refcounted sharer registry for last-sharer teardown.
- **NET-NEW option:** since we HAVE a push channel, consider PULLING creds over it — eliminates the env-blob inspect/`/proc` exposure window scion accepts.

### AVOID (their explicit mistakes / anti-pillar choices)
- Floating `latest`/unpinned deps (the no-drift hole). · go-plugin/RPC for the adapter. · Compiling per-harness-varying logic into the shared binary (the **"dialect trap"** — forced kernel+image rebuild per new harness). · The whole scion coordination shape (pull + host-external no-ack injection + screen-scraping + closed 5-value status enum as the entire semantic surface — semantics live in the LLM's head). · One-shot pods + report-only heartbeat. · Exposing a half-built host-type (their CloudRunRuntime is an unimplemented stub). · Gratuitous per-runtime duplication (their podman = "duplicate from Docker"). · Live in-container file mutation as the update path. · Ephemeral state that doesn't survive restart (their Hub SQLite on /tmp — our postgres substrate already avoids this).

### Open design questions surfaced by the audit (next brainstorm fuel)
1. **Resilience authority (D-run):** self-heal in the kernel, platform-native (k8s/systemd/watchtower), or both layered — and how does it interact with our lease-expiry stall-detector so the two don't fight?
2. **Cross-harness target scope (D-plugin):** claude-only first, or claude+opencode+gemini+codex? Decides whether we need the full dialect + capability-matrix machinery now or can defer.
3. **Shim form/language (D-plugin):** declarative manifest ONLY, or manifest + a thin JS/TS adapter module loaded by the kernel? How much in data vs code?
4. **Secret transport (D-creds/D-coord):** PULL creds over the live push channel vs file-mount vs staged-env-blob?
5. **Tool-surface ↔ push relationship (D-plugin/D-coord):** is our tool surface an MCP server the kernel hosts, and how does push-injection relate to the harness MCP/tool plumbing without colliding with TUI input?
6. **Runtime target set + sequencing (D-run):** minimum viable FULLY-implemented host-type set (local + VM first?); commit to k8s/cloudrun later?
7. **Observability transport (D-tmux/D-coord):** ride telemetry on the same reconnecting channel as push-coordination, or keep them separate? (Our own memory warns against piggybacking coordination on a lossy pipeline.)

## Image model (Director, 2026-06-29)
- **Single image per harness-TYPE** (`@apnex/claude-harness`, later `@apnex/opencode-harness`); per-AGENT (lily/greg/…) = pure ENV injection (identity/role · Hub URL+token · git creds · worktree/branch · model/quota) — NOT separate images. Reproducible + uniform + scalable (spin an agent = run the image with ENV).
- **Entrypoint script** does the uniform boot: register (register-SPI) → establish Hub connection (kernel) → start tmux → launch the CLI.
- **tmux** for the detached / re-attachable / observable long-running session (adopt from scion).
- **Message-injection (our differentiator) must survive IN the container + be resilient** → the adapter-kernel (P1 connection-resilience) keeps the pushed Hub→harness channel alive inside the containerised, tmux'd CLI. P1 (kernel) ⟂ P2 (container) interlock.

## Related entities
mission-64 (M-Adapter-Streamline; `update-adapter.sh`, the npm-model) · bug-203 (host won't re-enumerate tool-surface) · idea-392 (live auto-refresh / no-stale-caches / opencode-parity) · idea-391 (system CLI→Hub REST) · idea-390 (agent⟂project separation) · bug-184 (version-honesty) · candidate_A (the SR adapter summit) · `@apnex` npm namespace.

---
*Brainstorm log — appended as discussions progress. Becomes a formal Idea→Design once the shape settles.*
