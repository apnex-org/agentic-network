# vision-synthesis.md — TOMBSTONE (mission-103 constitutional transition)

**Status: RETIRED.** This constitutional-gestalt commentary over the teleological-goal set is retired along with the tele layer.

## The living constitution now

- **Axioms** `A0`–`A14` (`../mission-kit/axioms/`) — the composing vision (`A0`, the umbrella) plus its orthogonal sub-conditions. Read via `get_constitution` / `list_axioms` / `get_axiom`.
- **OrgCharter** (`get_charter`) — its **vision** section carries the org's distilled north-star (the role this synthesis used to play), and its bindings tie each axiom to a ratifying decision with provenance.

History is preserved in git; each axiom's Provenance section carries the lineage back to the goal it crystallized. Retained as a tombstone so inbound links resolve; no live semantics, no verb exposure.
