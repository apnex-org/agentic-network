# mission-103 P2 Council — Clash Divergence Map

**Inputs:** position-architect.md · position-engineer.md · position-verifier.md (all independent, verified by commit/publish order)
**Convergences (no clash needed):** A1 poll + B1 HEAD-with-sha · atomic swap · fail-open-stale · fail-closed-malformed · zero axiom write verbs · provenance-beside-content · typed storage over generic doc rows · additive-before-destructive sequencing · steve's contract-test set as floor · A3 read-through dead · webhook deferred.

| # | Divergence | Architect | Engineer | Verifier | Resolution + movement |
|---|---|---|---|---|---|
| D1 | Storage granularity | per-axiom `Axiom` rows + `Charter` singleton | ONE `ConstitutionSnapshot` singleton row (whole corpus; atomicity = single-row CAS) | dedicated snapshot kind + CAS active pointer + manifestHash | **Engineer wins; architect MOVES.** Per-row storage cannot swap atomically (the substrate's CAS unit is the row) — my shape re-created the mixed-version-constitution hazard all three seats banned. Synthesis: `ConstitutionSnapshot` singleton (E-1) + steve's `manifestHash` + steve's versioned snapshot retention (prior rows kept for history, `current` pointer semantics via the singleton id). |
| D2 | Charter home | Hub-native entity, mutations ONLY via rail-executed registry actions | git-canonical (charter/ files in the synced repo), rail-governance via proof-citing binding records + Director merge; zero hub write paths | Hub-native versioned `OrgCharter` rows, rail-required mutation, append-only | **Resolved by RATIFIED CONSTRAINT, not taste: T1 (G0, decision-14) says Hub-native.** Also structural: org charter files inside project-agnostic mission-kit would violate the kit's own A14-lesson quarantine. Engineer's underlying concern (a second write authority + bespoke verb surface) is honored in full: NO free-form charter verbs exist — mutation is exclusively rail-decision-executed registry actions (B5 machinery, already built and verified), satisfying steve's no-raw-writes + binding-exactness + append-only tests. Engineer's proof-citing binding-record FIELD SCHEMA (E-3) is adopted verbatim — it is the best-specified artifact of the clash. |
| D3 | First boot | bootstrap seed from build-time copy (availability over explicitness) | loud `not_synced` error, structurally distinct from empty | `not_yet_synced` error; bootstrap acceptable only if labeled + immediately superseded | **Architect MOVES to the error posture.** A fabricated-looking constitution at cold start is worse than an honest absence — and the window is engineered to seconds by a sync-on-start tick, making bootstrap seeding moot. Steve's labeled-bootstrap clause retained as a deploy-pipeline option, not a default. |
| D4 | Whole-corpus read | one-call `get_charter`, per-axiom reads | adds `get_constitution` (manifest + all bodies + charter, ONE call — the cold-start verb) | responses must be self-sufficient for cold start | **Engineer's `get_constitution` adopted.** It is the strongest recall-proofness answer (C7: one call = whole law); list/get/get_charter remain as cheap projections. |
| D5 | Director-profile churn | two-speed: durable summary rail-governed inline; living doc by pointer | (via D2) every profile touch = PR + Director merge | append-only versions, rail-required mutation, binding exactness | **Architect's two-speed split stands** (with D2's Hub-native home): the charter carries the rail-amended durable SUMMARY + a pointer to the living methodology doc; steve's append-only + exactness tests apply to the summary; the living doc stays ordinary-doc-governed. Rationale: constitutional stability for what a cold agent must recall, without turning every preference nuance into a Director ceremony — the T5-boundary logic applied to his own profile. |

**Director tie-breaks required: NONE.** D2 resolved by his own G0 ratification; every other divergence resolved by argument with recorded movement. G2 is the only Director touchpoint this phase.

**Slice plan:** engineer's S0–S5 (additive-before-destructive) adopted over architect's X1–X7 — same content, cleaner destructive boundary; verifier's three gate-tightenings (live tool-catalog negative proof, hub-doc gate under the bug-232 caveat, post-deploy cold-start probe) bound into S4/S5 as gate conditions.
